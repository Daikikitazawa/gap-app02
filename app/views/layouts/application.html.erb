<!DOCTYPE html>
<html>
  <head>
    <title>GapApp02</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="header-logo"><%= link_to("Gap", "/") %></div>
      <div class="header-menu">
        <ul>
          <% if session[:user_id] %>
             <li><%= link_to("新規投稿", "/posts/new") %></li>
             <li><%= link_to("投稿一覧", "/posts/index") %></li>
             <li><%= link_to("ログアウト", "/logout", {method: "post"}) %></li>
             <li>ユーザーID：
          <%= session[:user_id] %>
             </li>
          <% else %>
             <li><%= link_to("Gapについて", "/about") %></li>
             <li><%= link_to("新規登録", "/signup") %></li>
             <li><%= link_to("ログイン", "/login") %></li>
          <% end %>
        </ul>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
